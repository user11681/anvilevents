version = "0.6.0"

dependencies {
    compileOnly "${project.jsr}"

    modApi "${project.anvil}"
    include "${project.anvil}"

    modImplementation "${project.modmenu}"

    modImplementation "${project.usersmanual}"
}

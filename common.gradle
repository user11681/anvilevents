apply plugin: "maven"
apply from: "project.gradle"

group = "transfarmer"

repositories {
    maven {
        url "https://jitpack.io"
    }
    mavenLocal()
}

configurations.all {
    resolutionStrategy.cacheChangingModulesFor 0, 'seconds'
}
